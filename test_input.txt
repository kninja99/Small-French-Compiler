fonction factorial(ent i , ent j) {
    # ent i;
    # ent i -> functioncall(i,j);
    # functioncall(i,j);
    # ent k -> 5;
    k -> 5 * 2 + 1;
    si(true) {
        # do some code 
    }
    autre {
        # do some code
    }

    tandis(10 < 5) {
        # do some code
        si(true) {
            # do some code 
        }
        tandis(true){
            # do some code
        }
    }

    faire {
        # do some code
    } tandis(10 < 5);

    functioncall(2);
    revenir functioncall(2) + functioncall(i);
}
